# Password Vault (MVP)

Tech: Next.js (TypeScript), MongoDB, Mongoose, crypto-js (client AES)

## Quick start

1. Copy `.env.example` to `.env.local` and fill in values:
   - `MONGODB_URI`
   - `JWT_SECRET`

2. Install:
```bash
npm install

